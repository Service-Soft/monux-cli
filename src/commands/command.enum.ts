/**
 * All commands that the cli supports.
 */
export enum Command {
    HELP = 'help',
    H = 'h',
    VERSION = 'version',
    V = 'v',
    INIT = 'init',
    I = 'i',
    ADD = 'add',
    A = 'a'
}