/**
 * Replica of the NavElementTypes enum from ngx-material-navigation.
 * Is needed because the original requires the angular compiler.
 */
export enum NavElementTypes {
    TITLE_WITH_INTERNAL_LINK = 'titleWithInternalLink',
    INTERNAL_LINK = 'internalLink'
}